'use strict';

angular.module('dashboard.controller', [])

    .controller('dashboardCtrl', function ($scope, $location, Auth) {
 /*           $scope.applicationName = 'HSM';

            $scope.getHsmUrl = function (relativeUrl) {
                return $location.protocol() + '://' + $location.host() + ':'
                       + $location.port() + relativeUrl;
            };

            $scope.logout = function ()
            {
                Auth.logout();
                $location.path('/').replace();
            }*/

    });
